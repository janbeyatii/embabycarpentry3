# Embaby Carpentry - Environment Variables Template
# Copy this file to .env.local: cp .env.example .env.local
# Then fill in your actual values

# Admin Authentication
# Generate ADMIN_PASSWORD_HASH: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('YOUR_PASSWORD', 10).then(hash => console.log(hash))"
# Generate JWT_SECRET: node -e "const crypto = require('crypto'); console.log(crypto.randomBytes(64).toString('hex'))"
ADMIN_PASSWORD_HASH=your_bcrypt_hash_here
JWT_SECRET=your_random_jwt_secret_here

# Email Configuration (SMTP)
# Gmail: smtp.gmail.com, Port: 587
# Outlook: smtp-mail.outlook.com, Port: 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your_email_password_or_app_password
SMTP_FROM=your-email@gmail.com
ADMIN_EMAIL=admin@example.com

# Database Configuration
# Get from Vercel Dashboard â†’ Storage â†’ Your Neon Database
POSTGRES_URL=postgresql://user:password@host:port/database?sslmode=require
